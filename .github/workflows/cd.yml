name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v3
    - name: Deploy with Docker Compose
      run: |
        docker-compose down
        docker-compose up -d --build
